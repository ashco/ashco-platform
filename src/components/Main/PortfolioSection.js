import React from 'react';

const PortfolioMain = ({ projects }) => <section>Hi</section>;

// const PortfolioMainWrapper = styled.section`
//   border: 1px solid ${props => props.theme.colorPrimary};
// `;

export default PortfolioMain;
